chore: 合并 fix/build-typecheck-and-perf 分支的 TS 类型修复、打包性能优化、工程化改进

1. 打包体积优化
   highlight.js 全量 912KB→按需注册 23 种语言 105KB（新增 src/utils/hljs.ts）
   echarts/zrender 独立拆包，不混入页面 chunk（vite.config.ts）

2. 后端地址抽成环境变量
   config.ts 的 REAL_BACKEND_URL / MOCK_SERVER_URL 改为读 .env，部署换地址不用改代码
   .env.development / .env.production 新增 VITE_MOCK_SERVER_URL

3. alert 改通知组件
   request.ts 的 showMessage 从 alert() 改为 useNotification()，错误统一走 NotificationContainer
   App.vue 挂载该组件

4. 路由守卫补了两个检查
   没登录访问主页→跳 /login；登录了没选兴趣→跳 /interest；补了之前缺失的 next() 回调
   Login.vue 登录后自动检查兴趣状态，未选则引导去 /interest

5. Header 修 401
   fetchRegistrationTime 加 token 判断，没登录就不调 /api/user/info，刷新页面不再爆 401

6. 清 TS 类型告警
   ResetPassword.vue passwordRules.value→passwordRules（computed 非 ref）
   Home.vue 删未用 computed + void loadMoreTrigger
   Category.vue 删未用 ArticleListItem
   Settings.vue 删未用 MOCK_USER_PROFILE
   InterestSetup.vue 删未用 ParticleBackground
   ForgotPasswordModal.vue 删未用 reactive
   LeftSidebar.vue 修正 ../../constants→../constants
   markdown.ts / MarkdownRenderer.vue 改用 ./hljs 替代 import('highlight.js')
   vite.config.ts 删除重复的 /api/articles 代理
